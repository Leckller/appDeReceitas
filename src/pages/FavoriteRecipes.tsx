function FavoriteRecipes() {
  return (
    <div>
      {/* <button data-testid="filter-by-all-btn">All</button>
      <button data-testid="filter-by-meal-btn">Meals</button>
      <button data-testid="filter-by-drink-btn"> Drinks</button>
      <img src="" alt="" data-testid={`${index}-horizontal-image`}/>
      <p data-testid={`${index}-horizontal-image`}>text categoria</p>
      <p data-testid={`${index}-horizontal-image`}>text name</p>
      <button data-testid={`${index}-horizontal-image`}>compartilhar</button>
      <button data-testid={`${index}-horizontal-image`}>favoritar</button> */}

    </div>
  );
}

export default FavoriteRecipes;
